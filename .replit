# CinemaOS Frontend — Replit configuration
run = "npm run dev"

[nix]
channel = "stable-24_05"

[env]
NODE_ENV = "development"

[[ports]]
localPort = 3000
externalPort = 80

[deployment]
run = ["sh", "-c", "npm install && npm run build && npm run start"]
deploymentTarget = "cloudrun"
